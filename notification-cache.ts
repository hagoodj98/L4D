import { CacheNotificationState } from "./types/types.js";
// Map to store notification states for each user
export const cachedUserNotificationState: CacheNotificationState = new Map(); // Map to store
